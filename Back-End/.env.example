# MongoDB Connection String
# Get this from MongoDB Atlas or your local MongoDB instance
MONGODB_URI=mongodb://localhost:27017/pastebin-lite

# Server Port (optional, defaults to 5000)
PORT=5000

# Frontend URL (optional, defaults to http://localhost:3000)
# This should be the URL where users access your frontend application
# For local development: http://localhost:3000
# For production: https://your-frontend-domain.vercel.app
BASE_URL=http://localhost:3000

# Test Mode (optional, for automated testing)
# Set to "1" to enable test mode with deterministic time
TEST_MODE=
